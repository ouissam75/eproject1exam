{% extends 'base.html.twig' %}

{% block title %}Salaries{% endblock %}

{% block body %}
    <h1>Salaries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salary.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_salaries_index') }}">back to list</a>

    <a href="{{ path('app_salaries_edit', {'id': salary.id}) }}">edit</a>

    {{ include('salaries/_delete_form.html.twig') }}
{% endblock %}
